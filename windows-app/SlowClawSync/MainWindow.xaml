<Window
    x:Class="SlowClawSync.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="SlowClaw Sync">
    <Grid Padding="32" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="SlowClaw Sync"
                   FontSize="24"
                   FontWeight="SemiBold"/>
        <TextBlock Grid.Row="1"
                   Text="Scan this code with the SlowClaw iOS app to sync your journals."
                   Foreground="Gray"
                   TextWrapping="Wrap"/>

        <!-- The QR bitmap is rendered in code-behind (QRCoder) and set here. -->
        <Image Grid.Row="2"
               x:Name="QrImage"
               Stretch="Uniform"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               MaxWidth="360"
               MaxHeight="360"/>

        <StackPanel Grid.Row="3" Spacing="4">
            <TextBlock x:Name="StatusText" Text="Starting listener..." Foreground="Gray"/>
            <TextBlock x:Name="CountText" Text="" Foreground="Gray"/>
        </StackPanel>
    </Grid>
</Window>
