/target
firmware/*/target
*.db
*.db-journal
.DS_Store
# Vite build output (regenerated by `npm run build`; tracked only the index.html
# stub previously, which changed its asset hash on every build and dirtied the
# tree). The deploy workflow builds fresh before copying web/dist → _site/app.
web/dist/
._*
.wt-pr37/
__pycache__/
*.pyc
docker-compose.override.yml

# Environment files (may contain secrets)
.env

# Python virtual environments

.venv/
venv/

# ESP32 build cache (esp-idf-sys managed)

.embuild/
.env.local
.env.*.local

# Secret keys and credentials
.secret_key
*.key
*.pem
AuthKey_*.p8
credentials.json
.worktrees/
